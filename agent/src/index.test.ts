// This test is just a placeholder. It should be removed
// when real tests are needed. The package needs a single
// test in order for the test suite to run across the workspace

test("placeholder test", () => {
  expect(1 + 1).toEqual(2);
});
