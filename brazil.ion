'brazil_package_spec@1.0'

common::{
  name: "AmazonConnectSDK",
  major_version: "1.0",

  dependencies: {
    default_closure: run,

    closures: {
      run: public::{
        include: [self],
      },
    },
  },

  build: {
    command: "./build.sh",

    env: {
       PATH: [ (farm "PeruNPM" "bin"), (env PATH) ],
    },

    outputs: {
      public_dir: null,
      private_dir: null,
    },

    cleaned: [],
  },
}
