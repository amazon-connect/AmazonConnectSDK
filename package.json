{
  "name": "@amzn/amazon-connect-sdk",
  "version": "1.0.0",
  "description": "",
  "npm-pretty-much": {
    "useWorkspaces": true
  },
  "scripts": {
    "agent": "npm run --prefix ./agent",
    "app": "npm run --prefix ./app",
    "build": "npm run --ws build",
    "check-types": "npm run --ws check-types",
    "clean": "npm run --ws clean",
    "core": "npm run --prefix ./core",
    "test": "npm run --ws build && npm run --ws check-types && npm run --ws test"
  },
  "devDependencies": {
    "@babel/core": "^7.22.5",
    "@babel/preset-env": "^7.22.5",
    "@babel/preset-typescript": "^7.22.5",
    "@types/jest": "^29.5.2",
    "babel-jest": "^29.5.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.7",
    "ts-loader": "^9.4.2",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "core",
    "app",
    "agent"
  ]
}
